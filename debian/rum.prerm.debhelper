# Automatically added by dh_installinit
if ([ -x "/etc/init.d/rum" ] || [ -e "/etc/init/rum.conf" ]) && \
   [ "$1" = remove ]; then
	invoke-rc.d rum stop || exit $?
fi
# End automatically added section
